#include <stdio.h>        // it is a header file library that lets us work with input and output functions, such as printf() (used in line 4). Header files add functionality to C programs.

int main() {              //main(). This is called a function. Any code inside its curly brackets {} will be executed.
  printf("Hello World!"); //printf() is a function used to output/print text to the screen. In our example, it will output "Hello World!".   //Every C statement ends with a semicolon ;


//To insert a new line, you can use the \n character:
  printf("Hello Krishu!\n");
  printf("I am learning C.");

    printf("Hello World!\nI am learning C.\nAnd it is awesome!"); //You can also output multiple lines with a single printf() function. However, this could make the code harder to read:

    
//   \t	Creates a horizontal tab	
//   \\	Inserts a backslash character (\)	
//   \"	Inserts a double quote character
    return 0;               //return 0 ends the main() function.
}












